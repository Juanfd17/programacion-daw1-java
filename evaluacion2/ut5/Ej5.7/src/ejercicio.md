# Clase random
1. Esta en el paquete Java.util
2. Para crear una instacia Random() o Random(long seed)
3. Devuelve el siguiente valor pseudoaleatorio distribuido uniformemente int de la secuencia de este generador de números aleatorios. El contrato general de nextIntes que un intvalor se genera y se devuelve pseudoaleatoriamente. Todos los 2 32 valores posibles intse producen con (aproximadamente) la misma probabilidad.

# Clase Math
1. Java.lang
2. Devuelve el valor mas cercano
3. El valor doble que está más cerca que cualquier otro de pi, la relación entre la circunferencia de un círculo y su diámetro.

# Metodo arraycopy()
1. copia un array en otro
2. Compara dos cadenas lexicográficamente. La comparación se basa en el valor Unicode de cada carácter de las cadenas. La secuencia de caracteres representada por este objeto String se compara lexicográficamente con la secuencia de caracteres representada por la cadena de argumentos. El resultado es un entero negativo si este objeto String precede lexicográficamente a la cadena del argumento. El resultado es un entero positivo si este objeto String sigue lexicográficamente a la cadena del argumento. El resultado es cero si las cadenas son iguales; compareTo devuelve 0 exactamente cuando el método equals(Object) devolvería verdadero.

# API
1. el sort, el equals, toString